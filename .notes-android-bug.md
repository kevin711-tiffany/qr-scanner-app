# Android APK 掃描頁「請先在設定中輸入傳送網址」bug 修正記錄（2026-07-18）

## 使用者回報
- Android APK：設定頁已完成設定、查詢功能正常，但掃描頁掃完按送出出現「請先在設定中輸入傳送網址」
- 後續補充：iPhone 用 Expo Go 一切正常，Android（APK）無法運作

## 根因分析
- scan.tsx / query.tsx / settings.tsx 各自呼叫 useBasicSettings()，各 hook 實例有獨立 state
- 掃描頁的 settings state 只在 mount 時載入一次；在 APK（正式版）中分頁保持掛載，
  設定頁儲存後掃描頁的 state 不會更新，且 mount 時 AsyncStorage 讀取時機差異
  造成掃描頁 sendUrl 為空 → 誤報

## 已完成修正
1. hooks/use-basic-settings.ts：新增 getStoredSettings()（送出時直接讀 AsyncStorage 最新值）、
   匯出 loadSettings
2. app/(tabs)/scan.tsx：useFocusEffect 中呼叫 loadSettings()；handleComplete 改用
   await getStoredSettings() 取得 currentSettings 再檢查 sendUrl 與組參數
3. app/(tabs)/query.tsx：同樣修正（focus 時 loadSettings、handleQuery 用 getStoredSettings）

## 待辦
- tsc 通過（0 errors）
- 存 checkpoint 並提醒使用者重新 Publish 打包 APK 測試

## 其他背景
- APP 名稱：禾詰企業物料管理；最新 checkpoint（修正前）：bb504f14
- usetype 逗號防護已在 hooks/use-web-view.ts 的 toPlainValue 完成（去尾端逗號、陣列取首值）
- 傳送格式：application/x-www-form-urlencoded POST，PHP $_POST 讀取
