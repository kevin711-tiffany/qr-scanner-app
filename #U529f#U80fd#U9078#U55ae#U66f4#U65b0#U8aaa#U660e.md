# 功能選單更新說明

## 已完成

- 「掃描、單獨掃描、查詢、設定」四個頁面共用右上角功能選單按鈕。
- 按鈕可視尺寸 40 × 40 px，觸控區域 48 × 48 px。
- 每次點擊都從 AsyncStorage 讀取最新設定。
- 使用 HTTP POST，Content-Type：`application/x-www-form-urlencoded`。
- POST body：`code`、`remark1`、`remark2`、`remark3`、`usetype=E`。
- `sendUrl` 僅作為 POST 目的網址，不放入 POST body。
- 伺服器回應以全螢幕功能選單視窗顯示，並提供「關閉」按鈕。
- 已加入未設定網址、連線失敗與防止重複點擊處理。

## 主要修改檔案

- `components/function-menu-button.tsx`（新增）
- `components/app-header.tsx`（加入共用功能選單按鈕）

## Codespaces 更新與測試

1. 將 ZIP 解壓縮後的內容覆蓋 Codespaces 專案根目錄。
2. 在終端機執行：

```bash
pnpm install
pnpm check
npx expo start --tunnel
```

3. 使用 Expo Go 掃描終端機顯示的 QR Code。

## 版頭版尾修正
- 功能選單改為透明 overFullScreen Modal。
- Web 內容只顯示在版頭與底部分頁列之間。
- 上方保留 Safe Area + 60px 版頭。
- 下方保留 60px 分頁列 + Bottom Safe Area。

## 2026-07-25：移除關閉工具列並保持底部分頁可操作

- 移除功能選單使用的全螢幕 Modal。
- 功能選單改為 Tabs 內的隱藏頁面 `app/(tabs)/function-menu.tsx`。
- 不再顯示「功能選單／關閉」那一列。
- 固定 AppHeader 與底部五個功能分頁會持續顯示。
- 使用者可直接點擊底部「首頁、掃描、單獨掃描、查詢、設定」切換頁面。
- POST 規格維持 `code、remark1、remark2、remark3、usetype=E`。
