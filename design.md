# QR Code 連續掃描器 — 介面設計

## 設計理念

本應用程式以「快速、連續、即時」為核心，讓使用者能不間斷地掃描多個 QR Code，並一鍵將所有結果以逗號分隔傳送到指定網址。整體設計遵循 Apple HIG，採用簡潔的單色調搭配高對比掃描框，確保在光線不足環境下依然清晰易用。

## 色彩選擇

| 用途 | 色碼 | 說明 |
|------|------|------|
| Primary（品牌色） | `#2563EB` | 藍色，用於主要按鈕與掃描框 |
| Background | `#FFFFFF` / `#151718` | 白／深色背景 |
| Surface | `#F5F5F5` / `#1E2022` | 卡片背景 |
| Success | `#22C55E` | 掃描成功、傳送成功 |
| Error | `#EF4444` | 傳送失敗、錯誤提示 |
| Warning | `#F59E0B` | 重複掃描警告 |

## 畫面清單

### 1. 掃描畫面（Scan）— 首頁

**主要內容與功能：**
- 全螢幕相機預覽，中央顯示掃描框（圓角方形）
- 掃描框上方顯示已掃描數量（如「已掃描：5」）
- 掃描框下方顯示最近一次掃描內容（短摘要）
- 底部工具列：
  - 「清除」按鈕：清空所有已掃描結果
  - 「傳送」按鈕：將結果以逗號分隔傳送到指定網址
  - 「設定」按鈕：前往設定頁面
- 已掃描結果列表（可向上展開查看完整列表），每筆顯示序號與內容摘要，可滑動刪除單筆

**關鍵使用者流程：**
1. 開啟 App → 自動請求相機權限 → 進入掃描畫面
2. 對準 QR Code → 自動掃描 → 震動回饋 + 音效 → 結果加入列表
3. 重複掃描多個 QR Code → 列表持續累加
4. 點擊「傳送」→ 將所有結果以逗號分隔組合 → POST 到設定網址 → 顯示成功／失敗
5. 點擊「清除」→ 清空列表重新開始

### 2. 設定畫面（Settings）

**主要內容與功能：**
- 目標網址輸入框（Text Input）， placeholder 為 `https://example.com/api`
- HTTP 方法選擇（POST / GET），預設 POST
- 參數名稱輸入框，預設 `data`（傳送時的 query/body key）
- 測試連線按鈕：發送測試請求驗證網址可達
- 設定自動儲存至 AsyncStorage

**關鍵使用者流程：**
1. 從掃描頁底部點擊「設定」→ 進入設定畫面
2. 輸入目標網址 → 選擇 HTTP 方法 → 輸入參數名稱
3. 點擊「測試連線」→ 顯示連線結果
4. 返回掃描頁 → 設定已自動儲存

## 互動設計

| 元素 | 回饋方式 | 實作方式 |
|------|----------|----------|
| 掃描成功 | 震動 + 短暫綠色閃光 | `Haptics.notificationAsync(Success)` + 掃描框顏色動畫 |
| 傳送按鈕 | 縮放 + 震動 | `scale: 0.97` + `Haptics.impactAsync(Light)` |
| 清除按鈕 | 縮放 + 震動 | `scale: 0.97` + `Haptics.impactAsync(Light)` |
| 傳送成功 | 震動 + 綠色 Toast | `Haptics.notificationAsync(Success)` + Toast 訊息 |
| 傳送失敗 | 震動 + 紅色 Toast | `Haptics.notificationAsync(Error)` + Toast 訊息 |
| 重複掃描 | 短震動 + 黃色提示 | `Haptics.impactAsync(Light)` + Toast「重複的 QR Code」 |

## 螢幕方向

所有畫面均為 **直向（Portrait）**，適合單手操作。
