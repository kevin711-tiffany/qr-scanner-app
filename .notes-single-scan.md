# 單獨掃描功能開發筆記（2026-07-19）

## 使用者需求
1. [完成] 移除所有殘留「返回」文字：scan.tsx showWebView 頁與 settings.tsx showWebView 頁已移除（settings 掃描器頁的「返回設定」保留，使用者先前明確要求）
2. [進行中] 新增「單獨掃描」功能：掃描單一 QR Code 後，立即將資料 + usetype=D 傳到設定的傳送網址

## 實作計畫
- 新增 app/(tabs)/single-scan.tsx 分頁（標題「單獨掃描」，icon: qrcode.viewfinder → MaterialIcons "qr-code-scanner"）
- 參考 scan.tsx 架構：CameraView + isFocused 控制、useFocusEffect 重置、getStoredSettings() 送出時即時讀取
- 掃描到條碼即自動送出：params = { code, remark1, remark2, remark3, scannedData: 掃描值, usetype: 'D' }
- 送出成功後顯示 HtmlResponseView 結果頁；掃描中顯示「傳送中」overlay 防止重複觸發
- 首頁 index.tsx 加入「單獨掃描」功能卡片
- _layout.tsx 加入新 Tab（順序：首頁、掃描、單獨掃描、查詢、設定）
- icon-symbol.tsx 需先加 icon 映射

## 關鍵 API（既有）
- useBasicSettings(): { settings, loadSettings, getStoredSettings }
- useWebView(): { fetchWebContent(url, params) => Promise<boolean>, webViewData: {content, error} }
- usetype 值不可帶逗號（use-web-view.ts 已有防護）
